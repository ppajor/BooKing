import React, { Component } from "react";
import { StyleSheet, Text, View, TextInput, SafeAreaView } from "react-native";

export default () => {
  <View>
    <Text>SIEMA BYCZKU</Text>
  </View>;
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 30,
    backgroundColor: "#fff",
  },
});

export default Home;
